### Acceptance Demo Script
1) `make up`
2) `make chaos`
   - watch incident appear
   - investigate → approve → execute restart → verify resolved
3) `make attack`
   - watch incident appear
   - investigate → approve → execute block IP TTL → verify resolved

If you can do that on screen reliably, MVP is done.
